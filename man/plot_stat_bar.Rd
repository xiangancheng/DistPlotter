% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper_plots.R
\name{plot_stat_bar}
\alias{plot_stat_bar}
\title{绘制分组统计柱状图 (带标准误)}
\usage{
plot_stat_bar(data, group_var, value_var, error_type = "se")
}
\arguments{
\item{data}{数据框。包含分组变量 (group_var) 和测量变量 (value_var)。}

\item{group_var}{字符型。分组列名（如 "Treatment"）。}

\item{value_var}{字符型。测量值列名（如 "Expression"）。}

\item{error_type}{字符型。误差棒类型 ("se" 或 "sd")。}
}
\value{
ggplot 对象
}
\description{
适用于比较实验组和对照组的平均值，并用标准误 (SE) 表示不确定性。
}
\examples{
# 模拟实验数据
df_summary <- data.frame(
  Treatment = factor(rep(c("Control", "Drug_A", "Drug_B"), each = 20)),
  Measurement = rnorm(60, mean = rep(c(10, 15, 8), each = 20), sd = rep(c(1, 2, 1), each = 20))
)

plot_stat_bar(data = df_summary, group_var = "Treatment", value_var = "Measurement", error_type = "se")
}
