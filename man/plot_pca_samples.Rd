% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper_plots.R
\name{plot_pca_samples}
\alias{plot_pca_samples}
\title{绘制论文级 PCA 图 (Principal Component Analysis)}
\usage{
plot_pca_samples(data, metadata, group_var)
}
\arguments{
\item{data}{矩阵或数据框。行是样本 (Samples)，列是特征 (Genes/Features)。}

\item{metadata}{数据框。包含样本的元数据，必须有一列与 data 的行名匹配。}

\item{group_var}{字符型。用于分组着色的元数据列名（如 "Condition"）。}
}
\value{
ggplot 对象
}
\description{
用于展示样本间的聚类关系和批次效应。
}
\examples{
# 模拟数据：15个样本，100个特征
set.seed(42)
expr_matrix <- matrix(rnorm(1500), nrow = 15)
rownames(expr_matrix) <- paste0("Sample", 1:15)

# 模拟元数据：分为 Treatment 和 Control 两组
meta_data <- data.frame(
  SampleID = paste0("Sample", 1:15),
  Condition = factor(rep(c("Control", "Treatment"), each = 7.5))
)

plot_pca_samples(data = expr_matrix, metadata = meta_data, group_var = "Condition")
}
